<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
namespace:命名空间规则，具有唯一性。可以自己命名，但建议与Dao接口的全限定类名一致
-->
<mapper namespace="com.cshbxy.mapper.StudentMapper">
    <!--    查询所有记录-->
    <select id="findStudentByNameAndMajor" resultType="student">
        select * from dm_student where 1=1
        <choose>
            <when test="name!=null and name!=''">
                and name like concat('%',#{name},'%')
            </when>
            <when test="major!=null and major!=''">
                and major=#{major}
            </when>
            <otherwise>
                and sno is not null
            </otherwise>
        </choose>
    </select>
    <select id="findByList" parameterType="java.util.List" resultType="student">
        select *
        from mybatis.dm_student
        where id in
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
</mapper>